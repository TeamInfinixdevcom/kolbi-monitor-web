# Configuración de producción para Firebase Hosting

# Deshabilitara sourcemaps (menor tamaño de build)
GENERATE_SOURCEMAP=false

# Entorno
REACT_APP_ENV=production

# URL pública (dejar vacío si está en raíz)
PUBLIC_URL=/

# Nota: Firebase config se mantiene en src/firebase.js 
# Usa process.env.REACT_APP_FIREBASE_* desde .env.local
