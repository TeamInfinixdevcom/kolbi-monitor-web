import React from "react";

function SupervisorDashboardTabs({ children }) {
  // El tab activo ahora lo gestiona el dashboard principal
  return <>{children}</>;
}

export default SupervisorDashboardTabs;
